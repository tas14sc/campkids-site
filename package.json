{
  "name": "campkids-site",
  "version": "3.0.0",
  "private": true,
  "description": "The Camp Kids — static website",
  "scripts": {
    "start": "serve . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
